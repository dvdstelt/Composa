# Shell scripts run in Git Bash on the Windows runners too, and bash refuses a script whose lines end
# in CRLF. A Windows checkout with core.autocrlf would otherwise convert every one of them.
*.sh text eol=lf
